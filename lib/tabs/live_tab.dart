import 'package:flutter/material.dart';

class LiveTab extends StatelessWidget {
  const LiveTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Live Content", style: TextStyle(color: Colors.white)),
    );
  }
}
